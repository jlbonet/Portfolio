from coffee_data import *

def coin_processor(cash_register, quarters=None, dime=None, nickles=None, pennies=None):
    
    # if quarters is None:
    #     quarters=  user_input_coin_select_validator("how many quarters?: ")
    
    # if dime is None:
    #     dime = user_input_coin_select_validator("how many dime?: ")
    
    # if nickles is None:
    #     nickles =user_input_coin_select_validator("how many nickles?: ")
    
    # if pennies is None:
    #     pennies = user_input_coin_select_validator("how many pennies?: ")
    
    # total_coin = ((quarters*0.25) + (dime*0.25) + (nickles*0.25) + (pennies*0.25))

    print(cash_register)
    
    # return total_coin

coin_processor()